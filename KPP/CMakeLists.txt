if("${MECH}" STREQUAL fullchem)
	add_subdirectory(fullchem)
endif()
if("${MECH}" STREQUAL custom)
	add_subdirectory(custom)
endif()
if("${MECH}" STREQUAL aerochem)
	add_subdirectory(aerochem)
endif()
if("${MECH}" STREQUAL Hg)
	add_subdirectory(aerochem)
	add_subdirectory(Hg)
endif()

